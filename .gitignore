# pixi environments
.pixi/*
!.pixi/config.toml

# Node / build artifacts
node_modules/
dist/
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store

# Playwright MCP scratch (screenshots / console logs)
.playwright-mcp/

# Image archive (full-res PNG masters / JPG fallbacks — not in bundle)
archive/

# Top-level debug screenshots (Playwright MCP sometimes saves to repo root)
# Real assets live in public/ or archive/; root-level *.png is always scratch.
/*.png
/*.jpg
/*.jpeg
/*.webp

# Local-only — do NOT commit (token / hooks symlink to parent project)
.mcp.json
.github/hooks/
AGENTS.md
