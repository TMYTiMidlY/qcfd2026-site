# pixi environments
.pixi/*
!.pixi/config.toml

# Node / build artifacts
node_modules/
dist/
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local-only — do NOT commit (token / hooks symlink to parent project)
.mcp.json
.github/hooks/
